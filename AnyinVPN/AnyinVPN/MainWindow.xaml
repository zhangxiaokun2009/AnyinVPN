<Window x:Class="AnyinVPN.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="AnyinVPN" Height="760" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Icon="app.ico">

    <Grid>
        <tb:TaskbarIcon x:Name="MyNotifyIcon" IconSource="app.ico" ToolTipText="AnyinVPN" TrayMouseDoubleClick="Show_Click">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="显示界面" Click="Show_Click"/>
                    <Separator />
                    <MenuItem Header="断开连接" Click="Disconnect_Click"/>
                    <MenuItem Header="彻底退出" Click="FullExit_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

        <Border CornerRadius="20" Background="#F5F6FA" Margin="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.2"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="25,20,0,0">
                    <TextBlock Text="AnyinVPN · 安隐" FontSize="22" FontWeight="Bold" Foreground="#2F3640"/>
                    <TextBlock x:Name="StatusLabel" Text="准备就绪" FontSize="12" Foreground="#7F8C8D"/>
                </StackPanel>

                <ListBox x:Name="NodeListView" Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="10,5">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Bg" Background="White" CornerRadius="12" Margin="5,5" Padding="15">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bg" Property="BorderBrush" Value="#3498DB"/>
                                                <Setter TargetName="Bg" Property="BorderThickness" Value="2"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Width="320">
                                <StackPanel Orientation="Horizontal">
                                    <md:PackIcon Kind="ShieldCheck" Foreground="#3498DB" VerticalAlignment="Center" Width="24" Height="24"/>
                                    <StackPanel Margin="15,0">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="15" Foreground="#2C3E50"/>
                                        <TextBlock Text="{Binding Protocol}" FontSize="11" Foreground="#95A5A6"/>
                                    </StackPanel>
                                </StackPanel>
                                <TextBlock Text="{Binding Latency}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#27AE60" FontWeight="Bold"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button x:Name="ConnectBtn" Content="开始连接" Width="140" Height="45" Click="Connect_Click" Background="#3498DB" Foreground="White" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="22"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button x:Name="DisconnectBtn" Content="断开" Width="100" Height="45" Margin="15,0,0,0" Click="Disconnect_Click" Background="#E74C3C" Foreground="White" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="22"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Row="3" Margin="0,0,0,20" VerticalAlignment="Top" HorizontalAlignment="Center">
                    <Border Width="300" Height="200" CornerRadius="10" ClipToBounds="True" Background="White">
                        <wv2:WebView2 x:Name="AdWebView" DefaultBackgroundColor="Transparent" />
                    </Border>

                    <TextBlock Text="AnyinVPN官网：vpn.2-7.help" 
                               HorizontalAlignment="Center" 
                               Margin="0,10,0,0" 
                               Foreground="#95A5A6" 
                               FontSize="13"/>
                </StackPanel>

                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="15" Click="Exit_Click" Background="Transparent" BorderThickness="0" Foreground="#BDC3C7"/>
            </Grid>
        </Border>
    </Grid>
</Window>